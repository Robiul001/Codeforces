#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,a;
    cin>>n;
    while(n--)
    {
        cin>>a;
        if(a==1)
        {
            cout<<-1<<endl;
            return 0;
        }
    }
    cout<<1<<endl;
    return 0;
}
